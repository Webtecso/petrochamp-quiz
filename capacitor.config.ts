import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.petrochamp.quiz',
  appName: 'Petrochamp Quiz',
  webDir: 'out/renderer'
}

export default config
